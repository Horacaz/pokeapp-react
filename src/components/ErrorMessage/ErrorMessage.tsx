import { Heading } from "@chakra-ui/react";
export default function ErrorMessage() {
  return (
    <Heading as="h2" size="lg">
      An error has occured, please refresh and wait a few seconds.
    </Heading>
  );
}
